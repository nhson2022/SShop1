<p style="color: green"><%= notice %></p>

<%= render @item %>

<div>
  <%= link_to "Edit this item", edit_item_path(@item) %> |
  <%= link_to "Back to items", items_path %>

  <%= button_to "Destroy this item", @item, data: {turbo_method: "delete", turbo_confirm: "Are you sure?"}, class: "btn btn-danger" %>
</div>
